# Memory Index

- [connect-pg-simple + esbuild bundling](connect-pg-simple-esbuild.md) — createTableIfMissing fails silently when the server is esbuild-bundled; create the session table via SQL instead.
- [Orval date field coercion](orval-date-coercion.md) — body/response `format: date` fields become real `Date` objects (coerce), but query-param date fields do not; handle each differently.
